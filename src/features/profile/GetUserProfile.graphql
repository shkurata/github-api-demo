query GetUserProfile($login: String!) {
    user(login: $login) {
        id
        avatarUrl(size: 300)
        email
        url
        name
        websiteUrl
        repositories {
            totalCount
        }
    }
}
